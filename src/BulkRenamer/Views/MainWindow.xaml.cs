using System.Windows;

namespace BulkRenamer.Views;

/// Zero logic in code-behind. All state and behaviour lives in MainViewModel.
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
